# WorkspaceBadge

The tenant/workspace identity chip. Reinforces the single-workspace context (MVP) at the top of primary surfaces and in page headers.

```jsx
<WorkspaceBadge name="Acme Analytics" />
```

Props: `name`, `label` (default "Workspace").
