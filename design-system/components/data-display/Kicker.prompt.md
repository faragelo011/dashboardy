# Kicker

The uppercase eyebrow above page titles and section labels — the signature rhythm of the "Technical Dense" system (11px, 600 weight, 0.14em tracking, muted).

```jsx
<Kicker>Administrative settings</Kicker>
<h1>Data connection</h1>
```

Props: `as` (element, default `p`).
