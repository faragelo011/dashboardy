/**
 * Mirrors `specs/005-saved-questions-collections/contracts/saved-questions.openapi.yaml`
 * (Dashboardy Feature 5). Phase 1 scaffold only - full DTOs land in Phase 2 (T018).
 */

/** Placeholder export so consumers can import from `@dashboardy/types` during setup. */
export type SavedQuestionsContractsScaffold = Record<string, never>;
