{
  "name": "@dashboardy/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "node -e \"process.exit(0)\"",
    "lint": "uv run --directory . ruff check .",
    "test": "uv run --directory . pytest -q"
  }
}
